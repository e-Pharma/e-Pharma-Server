const express = require("express");
const router = express.Router();

// const handler = require("../handlers/verification");
// const resetHandler = require("../handlers/password_reset");

// router.post("/send_verification", handler.sendVerification);
// router.post("/send_password_reset_link", resetHandler.sendPasswordResetLink);

module.exports = router;
